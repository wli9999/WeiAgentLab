# Add New Video From Wei Agent Lab

## Project

Video Create

```text
C:\Users\wli\Documents\video create
```

## Goal

Use Wei Agent Lab as the command center to plan, create, test, and document new meditation videos in the external `video create` project.

## Current Status

Ready.

## Known External Project State

* External path exists.
* External project is a Git repository.
* Existing video folders use per-project `README.md`, `requirements.txt`, `assets/`, `src/`, `outputs/`, `build/`, and `exports-local/`.
* `AmitabhaSutraMeditationProject/` project files are tracked and pushed to GitHub as of 2026-06-15.
* Rendered `outputs/*.mp4` files are ignored so video output stays on the laptop.

## Standard Workflow

Use `workflows/video-create-add-new-video.md`.

## Intake Needed For Next Video

* Video title:
* Source text:
* Desired duration:
* Languages on screen:
* Audio style:
* Visual style:
* Target output:

## Verification Notes

* Connection verified from Wei Agent Lab to external path.
* Existing external README files reviewed.
* Amitabha project files committed and pushed in external commit `d77e736`.
* External repo status is clean except ignored local build, output, virtualenv, cache, and tool folders.

## Next Action

Collect the details for the next video and create the new video project folder in `C:\Users\wli\Documents\video create`.
